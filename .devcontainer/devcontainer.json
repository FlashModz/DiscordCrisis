{
    "name": "DiscordCrisis Development",
    "image": "mcr.microsoft.com/devcontainers/javascript-node:18",
    "features": {
        "ghcr.io/devcontainers/features/github-cli:1": {},
        "ghcr.io/devcontainers/features/docker-in-docker:2": {
            "version": "latest",
            "moby": true
        }
    },
    "customizations": {
        "vscode": {
            "extensions": [
                "dbaeumer.vscode-eslint",
                "esbenp.prettier-vscode",
                "GitHub.copilot"
            ],
            "settings": {
                "terminal.integrated.defaultProfile.linux": "bash",
                "terminal.integrated.profiles.linux": {
                    "bash": {
                        "path": "/bin/bash"
                    }
                }
            }
        }
    },
    "postCreateCommand": "mkdir -p /workspaces/.codespaces/.persistedshare && npm install",
    "remoteUser": "node",
    "initializeCommand": {
        "createLogs": "mkdir -p ${localWorkspaceFolder}/.devcontainer/logs && touch ${localWorkspaceFolder}/.devcontainer/logs/vsserverhostlog.txt ${localWorkspaceFolder}/.devcontainer/logs/vsserverterminallog.txt"
    },
    "mounts": [
        "source=${localWorkspaceFolder},target=/workspace,type=bind,consistency=cached",
        "source=${localWorkspaceFolder}/.devcontainer/logs,target=/var/lib/docker/codespacemount/.persistedshare,type=bind"
    ],
    "runArgs": ["--cap-add=SYS_PTRACE", "--security-opt", "seccomp=unconfined"]
}
